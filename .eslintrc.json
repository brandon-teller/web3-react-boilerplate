{
  "extends": ["next/core-web-vitals", "plugin:import/warnings"],
  "plugins": ["unused-imports"],
  "rules": {
    "no-unused-vars": "off", // Disable the base rule to avoid conflicts
    "unused-imports/no-unused-imports": "error" // Enable the rule from the plugin
  }
}
